git clone (ssh link in git gub green button)


public static void main - needed to be a runnable class

long numbers are usually 64 or 32 signed integers

java has no unsigned integers

"" is a string
'' is a character

float is used with 7 decimals precision
double is 15 decimals
double is the standard


the output is single point precision
that means it is faster

float needs f or l for literals

need double int and 

mkdir porject-name
makes a directory

cd ~ goes to home directory

cd .. goes up a file directory

code -    opens vs code in the directory

explorer .    opens a new file explorer in the directory you are currently in 