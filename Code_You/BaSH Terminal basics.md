-l is an example flag that is used in a command to display detailed information in a command
example command would be: ls -l /home
this command is a command to detailed list home directory

cd is change directory
pwd is used to print current directory, to see where you are
ls is used to list the content of the directory you are in, or needs to be specified

touch example.txt
creates an empty txt file named example

 cp file1.txt file2.txt  copies file1.txt to a new file named file2.txt

mv is used to rename

rm is used to remove

#!/bin/bash command to let system know to use BaSH to interpret the script

echo   print to the terminal

chmod +x scriptname.sh
makes an executable script

./script.sh
executes the runnable script


